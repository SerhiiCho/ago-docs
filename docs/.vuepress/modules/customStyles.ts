export default `
    a.route-link-active {
        margin-bottom: -2px;
        border-bottom: 2px solid var(--c-text-accent);
    }

    /* todo: remove this after bug is fixed in vuepress */
    .navbar-items-wrapper a.route-link {
        margin-bottom: -2px;
        border-bottom: 2px solid var(--c-text-accent);
    }
`